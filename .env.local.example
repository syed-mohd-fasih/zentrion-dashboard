# URL where the Zentrion orchestrator-api is reachable.
# In-cluster dev (kubectl port-forward -n zentrion-system svc/zentrion-orchestrator 3001:3001)
# and local backend dev both default to http://localhost:3001.
NEXT_PUBLIC_API_URL=http://localhost:3001
